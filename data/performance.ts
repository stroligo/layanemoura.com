/** Capas com prioridade alta (preload + eager). Resto lazy. */
export const GALLERY_PRIORITY_COVER_COUNT = 8;

/** Máximo de preloads no `<head>` da home. */
export const GALLERY_HEAD_PRELOAD_COUNT = 6;

/** Cache longo para ficheiros estáticos versionados (segundos). */
export const STATIC_CACHE_MAX_AGE = 60 * 60 * 24 * 365;
